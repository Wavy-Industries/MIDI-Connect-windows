<Window x:Class="MinimalWindowsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MinimalWindowsApp"
        Title="MIDI Connect"
        Height="320" Width="220"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowActivated="True"
        Topmost="True">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <Style x:Key="DeviceListBox" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="MaxHeight" Value="120"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Style.Triggers>
                <Trigger Property="HasItems" Value="False">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBox">
                                <TextBlock Text="No devices" 
                                           Foreground="#888888"
                                           FontStyle="Italic"
                                           Margin="12,8"/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DeviceListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="4,1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3c3c3c"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#007acc"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBlock" x:Key="HeaderText">
            <Setter Property="Foreground" Value="#cccccc"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="8,8,8,4"/>
            <Setter Property="Opacity" Value="0.8"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="DeviceText">
            <Setter Property="Foreground" Value="#f1f1f1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="EmptyText">
            <Setter Property="Foreground" Value="#cccccc"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="Margin" Value="12,8"/>
            <Setter Property="Opacity" Value="0.6"/>
        </Style>
    </Window.Resources>
    <Border Background="#2d2d30" CornerRadius="4" BorderBrush="#555555" BorderThickness="1" Margin="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Connected" 
                       Grid.Row="0"
                       Style="{StaticResource HeaderText}"/>
            
            <ListBox Grid.Row="1" 
                     ItemsSource="{Binding ConnectedDevices}"
                     SelectedItem="{Binding SelectedConnectedDevice, Mode=TwoWay}"
                     Style="{StaticResource DeviceListBox}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" 
                                       Text="{Binding Name}" 
                                       Style="{StaticResource DeviceText}"/>
                            <TextBlock Grid.Column="1" 
                                       Text="▲" 
                                       Foreground="#00ff00"
                                       FontSize="10"
                                       Margin="4,0,2,0"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding IncomingTraffic, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBlock Grid.Column="2" 
                                       Text="▼" 
                                       Foreground="#ff0000"
                                       FontSize="10"
                                       Margin="2,0,0,0"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding OutgoingTraffic, Converter={StaticResource BoolToVisibility}}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource DeviceListBoxItem}">
                        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="ListBoxItem_PreviewMouseLeftButtonDown"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
            
            <TextBlock Text="Available" 
                       Grid.Row="2"
                       Style="{StaticResource HeaderText}"
                       Margin="8,8,8,4"/>
            
            <ListBox Grid.Row="3" 
                     ItemsSource="{Binding DiscoveredDevices}"
                     SelectedItem="{Binding SelectedDiscoveredDevice, Mode=TwoWay}"
                     Style="{StaticResource DeviceListBox}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" Style="{StaticResource DeviceText}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource DeviceListBoxItem}">
                        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="ListBoxItem_PreviewMouseLeftButtonDown"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
            
            <Separator Grid.Row="4" Margin="8,8"/>
            
            <TextBlock Grid.Row="5" 
                       Text="© 2024 Wavy Industries AS"
                       Foreground="#888888"
                       FontSize="10"
                       HorizontalAlignment="Center"
                       Margin="8,4"/>
            
            <Button Grid.Row="6" 
                    Content="Exit"
                    Width="60"
                    Height="22"
                    Margin="8,4,8,8"
                    HorizontalAlignment="Center"
                    Command="{Binding ExitCommand}"/>
        </Grid>
    </Border>
</Window>
